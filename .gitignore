# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
# .python-version is intentionally tracked so the project-pinned interpreter
# is shared across machines (uv reads it).

# Build artifacts
build/
dist/
*.so

# LaTeX
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz

# Notebooks
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local-only: symlink to iCloud-backed Stanford coursework folder
/Stanford

# Data and runs (anchored to repo root so /data/ doesn't shadow src/jacobigrad/data/)
/data/
/runs/
/checkpoints/
/wandb/
*.pt
*.ckpt
